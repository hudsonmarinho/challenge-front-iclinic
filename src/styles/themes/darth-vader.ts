import { colors } from '../tokens';

export default {
  page: {
    background: colors.dark,
    text: colors.clear,
  },

  button: {
    background: colors.clear,
    text: colors.dark,
  },

  back: {
    color: colors.clear,
  },
};
