import { colors } from '../tokens';

export default {
  page: {
    background: colors.clear,
    text: colors.primary,
  },

  button: {
    background: colors.primary,
    text: colors.clear,
  },

  back: {
    color: colors.primary,
  },
};
